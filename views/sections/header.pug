header.main-header
  .container.flex.align-center.justify-content-between
    if user
      .user
        a(href='/').flex.align-center
          img.user-img(src='/img/avatar.png' alt='avatar')
          span.user-name= user.login
      a.logout-btn(href='/users/logout') Logout
